% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wait_for_job.R
\name{wait_for_job}
\alias{wait_for_job}
\title{Wait for a Geocoding Job to Complete}
\usage{
wait_for_job(job_id, timeout = 600)
}
\arguments{
\item{job_id}{Character string. The geocoder job ID returned by
\code{submit_job()}.}

\item{timeout}{Numeric. Maximum time in seconds to wait for job completion.
Defaults to 600 seconds (10 minutes).}
}
\value{
Invisibly returns the \code{job_id} once the job reaches
\code{"resultsCreated"} status.
}
\description{
Polls the BC Address Geocoder batch service until a submitted job reaches
a completed state or a timeout is exceeded.
}
\details{
This function repeatedly checks job status at a controlled interval and
blocks execution until results are available.

The function polls the endpoint:
\code{jobs/{job_id}.json}

at intervals specified by the service response field
\code{secondsToWaitForStatusCheck}, with safeguards:
\itemize{
\item Minimum wait: 1 second
\item Maximum wait: 30 seconds
\item Default fallback: 5 seconds if missing
}

The function stops with an error if the elapsed time exceeds \code{timeout}.
}
\section{Status behaviour}{

The loop exits only when:
\itemize{
\item \code{jobStatus == "resultsCreated"} (success)
}
}

\examples{
\dontrun{
job_id <- submit_job("addresses.csv")
wait_for_job(job_id)
}

}
\seealso{
\code{\link{submit_job}}, \code{\link{geocoder_request}}
}
\keyword{internal}
