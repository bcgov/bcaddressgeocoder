% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/download_results.R
\name{download_results}
\alias{download_results}
\title{Download Geocoding Job Results}
\usage{
download_results(job_id, output_file)
}
\arguments{
\item{job_id}{Character string. The geocoder job ID returned by
\code{submit_job()}.}

\item{output_file}{Character string. Path to the file where results will
be saved (e.g., \code{"results.csv"}).}
}
\value{
Invisibly returns \code{output_file}.
}
\description{
Downloads the completed results of a BC Address Geocoder batch job and
writes them to a local file.
}
\details{
The function first retrieves the job metadata to locate the result file,
then downloads the result content and saves it as a binary file.

The function performs two steps:
\enumerate{
\item Queries \code{jobs/{job_id}/results.json} to obtain the result
resource URI.
\item Downloads the result file using HTTP digest authentication.
}

The downloaded file is written using \code{writeBin()} without modification.
}
\examples{
\dontrun{
job_id <- submit_job("addresses.csv")
wait_for_job(job_id)
download_results(job_id, "results.csv")
}

}
\seealso{
\code{\link{submit_job}}, \code{\link{wait_for_job}}
}
